-- ==========================================================
-- ECLIPSE UI LIBRARY V4.0 (MUDINHO0975 PRO)
-- ==========================================================
local EclipseLib = {}
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")
local lp = Players.LocalPlayer

-- Configurações Globais
local ThemeColor = Color3.fromRGB(0, 130, 255)
local BgColor = Color3.fromRGB(12, 12, 14)
local ElementColor = Color3.fromRGB(18, 18, 22)

-- Função de som local (só você ouve)
local function PlayBubbleSound()
    local sound = Instance.new("Sound")
    sound.SoundId = "rbxassetid://16076716" -- Som de bolha (substitua se quiser outro)
    sound.Parent = workspace -- Ou game.Players.LocalPlayer.PlayerGui
    sound:Play()
    game:GetService("Debris"):AddItem(sound, 1)
end

-- Sistema Anti-Snoop mantido
local ProtectedMeta = {
    __tostring = function() return "[ECLIPSE ENGINE V4.0] 🛡️ Mudinho0975 Protected." end,
    __metatable = "Acesso Negado."
}
setmetatable(EclipseLib, ProtectedMeta)

local function Create(class, props, parent)
    local obj = Instance.new(class)
    for i, v in pairs(props) do obj[i] = v end
    if parent then obj.Parent = parent end
    return obj
end

-- Função de arrastar aprimorada
local function MakeDraggable(gui)
    local dragging, dragStart, startPos
    gui.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true; dragStart = input.Position; startPos = gui.Position
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    gui.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)
end

function EclipseLib:CreateWindow(config)
    local HubName = config.Name or "Eclipse"
    local gui = Create("ScreenGui", {Name = "EclipseHubEngine", DisplayOrder = 9999999, ResetOnSpawn = false, Parent = CoreGui})

    -- 1. INTRO BOT NARRATIVA
    local IntroFrame = Create("Frame", {Size = UDim2.new(1,0,1,0), BackgroundColor3 = BgColor, ZIndex = 999, Parent = gui})
    
    local ProfilePic = Create("ImageLabel", {
        Size = UDim2.new(0, 100, 0, 100), Position = UDim2.new(0.5, -50, 0.3, -50),
        Image = Players:GetUserThumbnailAsync(lp.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420),
        BackgroundColor3 = Color3.new(0,0,0), Parent = IntroFrame
    })
    Create("UICorner", {CornerRadius = UDim.new(1,0)}, ProfilePic)
    
    local NameLabel = Create("TextLabel", {Size = UDim2.new(0, 300, 0, 50), Position = UDim2.new(0.5, -150, 0.4, 0), Text = lp.Name, TextColor3 = Color3.new(1,1,1), BackgroundTransparency = 1, Parent = IntroFrame})
    
    local BotText = Create("TextLabel", {Size = UDim2.new(0, 600, 0, 100), Position = UDim2.new(0.5, -300, 0.6, 0), Text = "", TextColor3 = ThemeColor, Font = Enum.Font.Code, TextSize = 18, BackgroundTransparency = 1, Parent = IntroFrame})
    
    local StartBtn = Create("TextButton", {Size = UDim2.new(0, 200, 0, 50), Position = UDim2.new(0.5, -100, 0.8, 0), Text = "INICIAR MUDINHO0975", BackgroundColor3 = ThemeColor, TextColor3 = Color3.new(1,1,1), Parent = IntroFrame})
    Create("UICorner", {CornerRadius = UDim.new(0, 8)}, StartBtn)

    StartBtn.MouseButton1Click:Once(function()
        StartBtn.Visible = false
        local msg = "Olá, antes de mostrar seus dados normal do roblox irei falar sobre Developer Mudinho0975. Apenas um Scripter comum... Ou será que não?"
        for i = 1, #msg do
            BotText.Text = msg:sub(1, i)
            task.wait(0.04)
        end
        task.wait(1)
        TweenService:Create(IntroFrame, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
        task.wait(0.5)
        IntroFrame:Destroy()
    end)

    -- 2. MAIN HUB (MÓVEL E MAIOR)
    local MainFrame = Create("Frame", {
        Size = UDim2.new(0, 650, 0, 450), Position = UDim2.new(0.5, -325, 0.5, -225),
        BackgroundColor3 = BgColor, Active = true, Visible = true, Parent = gui
    })
    MakeDraggable(MainFrame) -- Hub Móvel
    Create("UICorner", {CornerRadius = UDim.new(0, 8)}, MainFrame)
    Create("UIStroke", {Color = ThemeColor, Thickness = 2}, MainFrame)

    local Window = {}
    local Sidebar = Create("Frame", {Size = UDim2.new(0, 160, 1, 0), BackgroundColor3 = ElementColor, Parent = MainFrame})
    Create("UICorner", {CornerRadius = UDim.new(0, 8)}, Sidebar)

    function Window:CreateTab(name)
        local PageContainer = Create("Frame", {Size = UDim2.new(1, -170, 1, -20), Position = UDim2.new(0, 165, 0, 10), BackgroundTransparency = 1, Parent = MainFrame})
        local Page = Create("ScrollingFrame", {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, ScrollBarThickness = 2, Parent = PageContainer})
        Create("UIListLayout", {Padding = UDim.new(0, 8)}, Page)

        local Elements = {}
        function Elements:CreateButton(cfg)
            local btn = Create("TextButton", {Size = UDim2.new(1, -10, 0, 38), Text = "  " .. cfg.Name, BackgroundColor3 = ElementColor, TextColor3 = Color3.new(1,1,1), Font = Enum.Font.GothamSemibold, TextXAlignment = Enum.TextXAlignment.Left, Parent = Page})
            Create("UICorner", {CornerRadius = UDim.new(0, 6)}, btn)
            local stroke = Create("UIStroke", {Color = ThemeColor, Transparency = 1, Parent = btn}) -- Para o Blue Bloom

            btn.MouseButton1Click:Connect(function()
                PlayBubbleSound()
                -- Efeito Blue Bloom
                TweenService:Create(stroke, TweenInfo.new(0.2), {Transparency = 0}):Play()
                TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = ThemeColor}):Play()
                task.wait(0.2)
                TweenService:Create(stroke, TweenInfo.new(0.3), {Transparency = 1}):Play()
                TweenService:Create(btn, TweenInfo.new(0.3), {BackgroundColor3 = ElementColor}):Play()
                
                if cfg.Callback then task.spawn(cfg.Callback) end
            end)
        end
        return Elements
    end
    return Window
end

return EclipseLib
